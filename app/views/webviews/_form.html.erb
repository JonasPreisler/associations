<%= form_with(model: webview, local: true) do |form| %>
  <% if webview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webview.errors.count, "error") %> prohibited this webview from being saved:</h2>

      <ul>
      <% webview.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
